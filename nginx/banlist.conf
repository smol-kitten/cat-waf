# CatWAF Ban List
# Managed by auto-ban-service and fail2ban
# Format: geo block using $remote_addr
# Usage in server blocks: if ($ban) { return 403; }

geo $ban {
    default 0;
    # Entries added automatically:
    # 1.2.3.4 1;
}
